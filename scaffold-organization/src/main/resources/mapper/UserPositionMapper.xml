<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kite.organization.mapper.UserPositionMapper">

    <resultMap id="UserPositionMap" type="com.kite.organization.entity.UserPosition">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="position_id" property="positionId"/>
        <result column="primary_flag" property="primaryFlag"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByUserId" resultMap="UserPositionMap">
        SELECT id, tenant_id, user_id, position_id, primary_flag, create_time, update_time
        FROM org_user_position
        WHERE tenant_id = #{tenantId} AND user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO org_user_position(tenant_id, user_id, position_id, primary_flag, create_time, update_time)
        VALUES (#{tenantId}, #{userId}, #{positionId}, #{primaryFlag}, #{createTime}, #{updateTime})
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM org_user_position WHERE tenant_id = #{tenantId} AND user_id = #{userId}
    </delete>
</mapper>
