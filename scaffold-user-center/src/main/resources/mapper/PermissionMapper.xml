<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kite.usercenter.mapper.PermissionMapper">

    <resultMap id="BaseResultMap" type="com.kite.usercenter.entity.Permission">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="parent_id" property="parentId"/>
        <result column="path" property="path"/>
        <result column="method" property="method"/>
        <result column="icon" property="icon"/>
        <result column="component" property="component"/>
        <result column="visible" property="visible"/>
        <result column="status" property="status"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, code, name, type, parent_id, path, method, icon, component, visible, status, sort, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_permission (code, name, type, parent_id, path, method, icon, component, visible, status, sort)
        VALUES (#{code}, #{name}, #{type}, #{parentId}, #{path}, #{method}, #{icon}, #{component}, #{visible}, #{status}, #{sort})
    </insert>

    <update id="update">
        UPDATE sys_permission
        <set>
            <if test="name != null"> name = #{name},</if>
            <if test="type != null"> type = #{type},</if>
            <if test="parentId != null"> parent_id = #{parentId},</if>
            <if test="path != null"> path = #{path},</if>
            <if test="method != null"> method = #{method},</if>
            <if test="icon != null"> icon = #{icon},</if>
            <if test="component != null"> component = #{component},</if>
            <if test="visible != null"> visible = #{visible},</if>
            <if test="status != null"> status = #{status},</if>
            <if test="sort != null"> sort = #{sort},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_permission WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM sys_permission WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM sys_permission ORDER BY sort ASC, id ASC
    </select>

    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM sys_permission
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM sys_permission
        WHERE code = #{code}
    </select>

    <select id="countByParentId" resultType="int">
        SELECT COUNT(1)
        FROM sys_permission
        WHERE parent_id = #{parentId}
    </select>
</mapper>
